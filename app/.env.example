
# EXAMPLE ENVIRONMENT VARIABLES - COPY TO .env AND UPDATE WITH REAL VALUES

# Database Connection (Use managed Postgres for production like Neon, Supabase, or Railway)
DATABASE_URL="postgresql://username:password@host:port/database"

# AI API Key (This will be provided automatically in hosted environments)
ABACUSAI_API_KEY="your_api_key_here"

# NextAuth Configuration
NEXTAUTH_SECRET="your_generated_secret_here"
NEXTAUTH_URL="http://localhost:3000"  # Change to your production URL for deployment

# For production deployment on Vercel:
# 1. Set NEXTAUTH_URL to your production domain (e.g., https://waz-ai-cofounder.vercel.app)
# 2. Generate a new NEXTAUTH_SECRET using: openssl rand -base64 32
# 3. Use a managed Postgres service (Neon, Supabase, etc.) for DATABASE_URL
# 4. ABACUSAI_API_KEY will be provided automatically in production
